---
title: UI寸法計測の罠
description: フォント読み込み待ち・プローブ条件一致・@containerの順序・状態間の高さ一致
phases: [設計, 製造, 検収]
products: [Claude, Codex, Antigravity]
---

# UI寸法計測の罠

対象: `apps/web`のカード・ダッシュボード系レイアウトを実装・検収するとき。

**出典: `mj-stats-viewer`のカード系Issueでの差し戻し事例(同種のミスを5回踏んだ実績)。** wx-viewer-pocでカード・ダッシュボード系のレイアウトを実装・検収する段になったら、次を確認する。

1. 高さ計測は`await document.fonts.ready`の後に行う。Webフォント適用でテキストのメトリクスが変わり、先に測ると数px単位のズレを「実装の欠陥」と誤診しかねない。
2. devギャラリー等の寸法プローブは実ページと同じ条件(フィルタ状態・コンテンツ幅・スクロールバーの有無)にする。プローブが壊れていると検収が誤って合格を出す。
3. `@container`ブロックは基本ルールより後ろに置く。詳細度が同じ場合、基本ルールが後ろにあると後勝ちでコンテナクエリが打ち消される。
4. 状態間(loading/ready/error等)の高さ一致は共有変数(例: CSSカスタムプロパティ)で担保する。どちらかの状態の実測値を焼き付ける方式は構成変更のたびにズレる。

これらはすべて「エラーも警告も出ず、数値が静かにズレる」種類の失敗であり、通常のテストでは捕まらない。[02-design-protocol.md](../02-design-protocol.md)の必須項目(受け入れ条件に「何をどう実行して何が見えれば合格か」を書く)を満たす具体例として、計測手順(待機・ビューポート幅・スクロールバーの有無)まで書くとこの種の失敗を防げる。ブラウザ環境固有の検証制約は[C-01-browser-pane-visibility-limit.md](C-01-browser-pane-visibility-limit.md)を参照。
